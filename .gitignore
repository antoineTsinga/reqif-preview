node_modules/
dist/
*.log

# Local scratch scripts and their output — not part of the library.
src/try.ts
try.mjs
preview.html

# Large local test datasets. sample.reqif and test/sample1.reqif are the
# fixtures the test suite actually uses; anything bigger stays out of history.
AEB_Platform_Requirements_TestDataset.reqif
sample-multi-doc.reqifz
*.reqifz.local

# Docs-site build artefacts: copied from dist/ and regenerated by
# scripts/sync-docs-assets.mjs on every `npm run docs:build`. Committing them
# is how the previous documentation page went two months stale.
docs/public/reqif-preview.js
docs/public/exemple.reqifz
docs/.vitepress/dist/
docs/.vitepress/cache/
